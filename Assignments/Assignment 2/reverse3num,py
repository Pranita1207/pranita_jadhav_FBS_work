num= int(input("Enter a 3-digit number:"))

hundreds=num // 100
tens=(num // 10) % 10
ones = num % 10

reversed_num = (ones * 100) + (tens * 10) + hundreds

print(f'Reveresed number is :{reversed_num}')